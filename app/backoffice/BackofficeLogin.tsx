"use client";import{useState}from"react";import{signInBackoffice,type BackofficeMembership}from"@/lib/backoffice";
export function BackofficeLogin({onSuccess}:{onSuccess:(x:BackofficeMembership)=>void}){const[e,setE]=useState("");const[p,setP]=useState("");const[error,setError]=useState("");return <main className="bo-login"><section><img src="/medibridge-logo.svg" alt="MediBridge"/><h1>Backoffice</h1><form onSubmit={async event=>{event.preventDefault();try{onSuccess(await signInBackoffice(e,p))}catch{setError("Anmeldung nicht möglich.")}}}><label>E-Mail<input type="email" value={e} onChange={x=>setE(x.target.value)}/></label><label>Passwort<input type="password" value={p} onChange={x=>setP(x.target.value)}/></label>{error&&<p className="bo-error">{error}</p>}<button>Anmelden</button></form></section></main>}
