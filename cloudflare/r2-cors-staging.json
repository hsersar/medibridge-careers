{
  "rules": [
    {
      "allowed": {
        "origins": [
          "http://127.0.0.1:3000",
          "https://medibridge-careers-staging.vercel.app"
        ],
        "methods": ["GET", "PUT", "HEAD"],
        "headers": ["Content-Type"]
      },
      "exposeHeaders": ["ETag"],
      "maxAgeSeconds": 3600
    }
  ]
}
